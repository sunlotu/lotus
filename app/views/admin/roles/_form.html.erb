<%= form_for([:admin,@role], html: { class: 'form-horizontal' }) do |f| %>
  <% if @role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@role.errors.count, "error") %> prohibited this role from being saved:</h2>

      <ul>
      <% @role.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :code, '代码', class: 'col-md-2 control-label' %>
    <div class="col-sm-8">
      <%= f.text_field :code, class: 'form-control', placeholder: '代码' %>
    </div>
  </div>

  <div class="form-group">
    <%= f.label :name, '名称', class: 'col-sm-2 control-label' %>
    <div class="col-sm-8">
      <%= f.text_field :name, class: 'form-control', placeholder: '名称' %>
    </div>
  </div>

  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-8">
      <%= f.submit '确定', class: 'btn btn-primary btn-block' %>
    </div>
  </div>
<% end %>
